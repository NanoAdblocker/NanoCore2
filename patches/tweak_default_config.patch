diff --git a/src/js/assets.js b/src/js/assets.js
index 233fa30..d7ee1a1 100644
--- a/src/js/assets.js
+++ b/src/js/assets.js
@@ -285,7 +285,7 @@ var registerAssetSource = function(assetKey, dict) {
         entry.contentURL = [];
     }
     if ( typeof entry.updateAfter !== 'number' ) {
-        entry.updateAfter = 5;
+        entry.updateAfter = 3;
     }
     if ( entry.submitter ) {
         entry.submitTime = Date.now(); // To detect stale entries
@@ -904,7 +904,7 @@ api.rmrf = function() {
 // Asset updater area.
 var updaterStatus,
     updaterTimer,
-    updaterAssetDelayDefault = 120000,
+    updaterAssetDelayDefault = 300000,
     updaterAssetDelay = updaterAssetDelayDefault,
     updaterUpdated = [],
     updaterFetched = new Set(),
diff --git a/src/js/background.js b/src/js/background.js
index d711a61..1f360dd 100644
--- a/src/js/background.js
+++ b/src/js/background.js
@@ -40,12 +40,12 @@ var µBlock = (function() { // jshint ignore:line
 
     var hiddenSettingsDefault = {
         assetFetchTimeout: 30,
-        autoUpdateAssetFetchPeriod: 120,
-        autoUpdatePeriod: 7,
+        autoUpdateAssetFetchPeriod: 300,
+        autoUpdatePeriod: 4,
         debugScriptlets: false,
         ignoreRedirectFilters: false,
         ignoreScriptInjectFilters: false,
-        manualUpdateAssetFetchPeriod: 500,
+        manualUpdateAssetFetchPeriod: 1,
         popupFontSize: 'unset',
         suspendTabsUntilReady: false,
         userResourcesLocation: 'unset'
@@ -56,6 +56,7 @@ var µBlock = (function() { // jshint ignore:line
         'chrome-extension-scheme',
         'chrome-scheme',
         'moz-extension-scheme',
+        'ms-browser-extension-scheme',
         'opera-scheme',
         'vivaldi-scheme',
         'wyciwyg-scheme',   // Firefox's "What-You-Cache-Is-What-You-Get"
diff --git a/src/js/start.js b/src/js/start.js
index e6099f4..8e3878c 100644
--- a/src/js/start.js
+++ b/src/js/start.js
@@ -164,6 +164,7 @@ var onVersionReady = function(lastVersion) {
         µb.permanentFirewall.fromString(fwRules, true);
         µb.savePermanentFirewallRules();
         µb.hnSwitches.fromString([
+            'no-csp-reports: * true',
             'no-large-media: behind-the-scene false'
         ].join('\n'), true);
         µb.saveHostnameSwitches();
